

export default function Cars_Delete(props)
{

   /* const url="https://localhost:7049/cars"

    const request=fetch(url+props.id).then(resp=>resp.json()).then()
    */
  
   console.log(props.id)
    return(
        <>
        </>
    )
}